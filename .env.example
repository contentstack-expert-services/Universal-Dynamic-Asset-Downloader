# AEM Asset Downloader - Environment Configuration
# Copy this file to .env and fill in your values

# ===========================================
# REQUIRED CONFIGURATION
# ===========================================

# Your AEM instance URL (without trailing slash)
# Example: https://author.mycompany.com or http://localhost:4502
BASE_URL=https://your-aem-instance.com

# Your AEM session cookie for authentication
# To get this:
# 1. Log into your AEM instance in a browser
# 2. Open Developer Tools (F12)
# 3. Go to Network tab
# 4. Make a request to AEM
# 5. Find the Cookie header in the request headers
# 6. Copy the entire cookie value
COOKIE=login-token=your-token-here; other-cookies=values

# Output directory for downloaded assets
# Can be absolute path or relative to script location
OUTPUT_DIR=./dam-downloads

# ===========================================
# OPTIONAL CONFIGURATION
# ===========================================

# File types to download (comma-separated, empty = all types)
# Example: jpg,png,pdf,docx
# FILE_TYPES=

# Minimum file size in bytes (0 = no minimum)
# MIN_FILE_SIZE=0

# Maximum file size in bytes (default: 5GB)
# MAX_FILE_SIZE=5000000000

# Maximum concurrent downloads (default: 3)
# MAX_CONCURRENT=3

# Delay between requests in milliseconds (default: 200)
# SLEEP_TIME=200

# Maximum folder depth to scan (default: 50)
# MAX_DEPTH=50

# Request timeout in milliseconds (default: 30000)
# REQUEST_TIMEOUT=30000

# Number of retry attempts for failed requests (default: 3)
# RETRY_ATTEMPTS=3

# Delay between retries in milliseconds (default: 1000)
# RETRY_DELAY=1000

# Download asset metadata as JSON files (true/false, default: true)
# DOWNLOAD_METADATA=true

# Download asset renditions (true/false, default: false)
# DOWNLOAD_RENDITIONS=false
